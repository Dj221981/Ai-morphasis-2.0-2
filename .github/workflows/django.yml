name: Django CI
# NOTE: This repository does not use Django. This workflow is kept for
# historical reference but is disabled. The active CI workflow is
# .github/workflows/tests.yml.

on:
  workflow_dispatch:  # manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Django CI is disabled for this repository. See .github/workflows/tests.yml."
