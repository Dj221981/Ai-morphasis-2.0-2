name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  # -----------------------------------------------------------------------
  # Lint — syntax errors and undefined names must block the build
  # -----------------------------------------------------------------------
  lint:
    name: Lint (flake8)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install flake8
        run: pip install flake8

      - name: Syntax and undefined-name check
        # E9xx  — syntax errors, F63x — invalid escape, F7xx — undefined names,
        # F82x  — undefined names (stricter).  These are real failures, not style.
        run: flake8 app src tests --count --select=E9,F63,F7,F82 --show-source --statistics

  # -----------------------------------------------------------------------
  # Test — must pass on all supported Python versions
  # -----------------------------------------------------------------------
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        # APP_ENV is required by the /ready endpoint; set it to a valid
        # value so that test_ready_with_valid_app_env passes in CI.
        # Individual tests that check invalid/missing values use monkeypatch
        # to override this environment variable for their scope.
        env:
          APP_ENV: development
        run: python -m pytest tests -v
